const users = require("./users");
const inventory = require("./inventory");
const patients = require("./patients");

module.exports = {
  users: users,
  inventory: inventory,
  patients: patients,
};
